{
  "name": "stg-web3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx hardhat test",
    "lint": "eslint . || true",
    "deploy": "npx hardhat run scripts/deploySTGToken.js --network $TARGET_NETWORK"
  },
  "devDependencies": {
    "hardhat": "^2.12.0",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "chai": "^4.3.7",
    "ethers": "^5.7.2",
    "eslint": "^8.0.0"
  }
}
